import { styled } from "@mui/material/styles";
import { Typography, IconButton } from "@mui/material/";

export const CampaignName = styled(Typography)`
  flex-grow: 1;
`;

export const LogoButton = styled(IconButton)`
  margin-right: 1rem;
`;
